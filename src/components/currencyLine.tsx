import React from "react";

const currencyLine = () => {

    // (rate.rate * 10).toLocaleString('en-US', {
    //     style: 'currency',
    //     currency: rate.currency,
    // })

    return (
        <div>
            
        </div>
    )
}

export default currencyLine
